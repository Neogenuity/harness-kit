<!-- PR description template. Wire it natively by copying to
     .github/pull_request_template.md, or paste per-PR. Keep every section —
     an honest "none" beats a deleted heading. -->

## What

<One paragraph: the change as the reviewer will experience it.>

## Why

<The problem or plan item this serves. Link the execution plan or issue —
docs/plans/active/<plan>.md — rather than restating it.>

## Verification

<What was actually run, with outcomes. "bash scripts/harness/verify green"
is the floor; name anything beyond it (new tests, fixtures, live checks) and
anything deliberately NOT verified.>

## Risk

<Blast radius if wrong, rollback path, and any follow-up owed (docs,
migration, monitoring). "Low — docs only" is a valid entry.>
