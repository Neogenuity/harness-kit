# v0.5.0 — Repackage to `plugins/harness-kit/` + Codex distribution

Status: **active**

## Objective

Move the distributed plugin from `plugin/` to `plugins/harness-kit/` and add
a Codex distribution channel, so the same source tree installs as a plugin
in both Claude Code and Codex.

## Value

Codex reads project skills from `.agents/skills/` today, and the kit
installs *into* Codex via the manual clone-and-copy path the README
documents — workable, but not versioned and not updatable in place. Codex
plugins (`.codex-plugin/plugin.json`, marketplaces at
`.agents/plugins/marketplace.json`, plugins conventionally under `plugins/`
— per Codex's plugin docs; re-verify and stamp at execution, see scope
item 8) are the versioned distribution channel, and their layout
conventions match a `plugins/harness-kit/` tree. One repackage buys a second
first-class install path with zero content duplication.

## Scope

1. `git mv plugin plugins/harness-kit` (internal refs are relative — survive).
2. Packaging: `plugins/harness-kit/VERSION` (single version source);
   `.claude-plugin/plugin.json` → 0.5.0; new
   `plugins/harness-kit/.codex-plugin/plugin.json` (name/version/description,
   `"skills": "./skills/"`); new root `.agents/plugins/marketplace.json`
   (local source `./plugins/harness-kit`); root `.claude-plugin/marketplace.json`
   source → `./plugins/harness-kit`.
3. Root tailored mechanism updates: `verify.sh` manifests gate extends to all
   four JSONs plus VERSION == both plugin.json versions;
   `test-template-sync.sh` TPL path; `guard-project-policy.sh` version reads
   from VERSION.
4. CI globs + `.cursor/rules/templates.mdc` paths.
5. Docs sweep (README + Codex install section, llms.txt, AGENTS.md,
   CONTRIBUTING, ARCHITECTURE, ADRs 004/006, conventions, release skill,
   kit SKILL.md, CHANGELOG 0.5.0 entry).
6. ADR 007 — dual-provider packaging (both manifests checked in verbatim;
   VERSION as neutral source; verify.sh consistency gate).
7. migrations.md — new playbook: "a provider ships a plugin/marketplace
   distribution channel".
8. Provider matrix: new **Distribution** row — Claude Code
   (`.claude-plugin/` manifest + marketplace, `/plugin marketplace add`) and
   Codex (`.codex-plugin/plugin.json`, `.agents/plugins/marketplace.json`,
   `codex plugin marketplace add`) — every fact re-verified against primary
   docs at edit time and stamped per ADR 004. The layout facts this plan
   rests on currently live nowhere citable in the repo; this item fixes
   that.
9. Optional hardening: protect `.agents/plugins/marketplace.json` +
   `.codex-plugin/*` in root guard-config.
10. Re-pin manifest (`# harness-kit 0.5.0`, markers preserved); sync; verify.

This file is the canonical scope and status. Re-derive the rename blast
radius at execution time with the verification grep below — the docs sweep
in item 5 enumerates the known citation sites.

### Riders (small, shipping alongside)

- **Docs correctness**: `references/pattern.md` still says `# tailored`
  manifest lines are *skipped* by integrity checking — stale since 0.4.0,
  when check #9 started verifying tailored checksums (the marker now only
  exempts a file from template replacement, not from pinning). Align the
  sentence with `check-harness.sh` and SKILL.md. Found by external review,
  validated 2026-07-10.

## Out of scope

Extracting init/update into a script; automated fresh-repo init test; real
Codex install smoke in CI (manual verification instead). Everything in the
queued plans.

## Dependencies

- PR #4 (v0.4.0 protocol correctness) merged — **done**.
- Entry criterion: real Codex payload capture — **done 2026-07-10** (see
  Decisions). The envelope differed from the schema-derived fixtures and the
  capture found a guard bypass, now fixed in **v0.4.1** (the correctness
  patch that precedes this plan). No `apply_patch|shell` alternation is
  needed. **v0.4.1 must merge before the rename lands.**

## Verification

`bash scripts/verify.sh` green (manifests gate proves 4 JSONs valid + 3-way
version equality); `git grep -n 'plugin/' | grep -v -- '-plugin/'` returns
only CHANGELOG history (the filter excludes the legitimate
`.claude-plugin/` / `.codex-plugin/` manifest paths, which contain the
substring by construction); `/plugin marketplace add` (Claude Code) and
`codex plugin marketplace add` (Codex) both resolve the plugin from the
branch; manifest header 0.5.0 == VERSION.

## Progress

- 2026-07-10 — Entry-criterion capture executed (Codex 0.144.1, authed). Real
  apply_patch payload is the **bare** envelope in `tool_input.command` (no
  `apply_patch` literal; identity in `tool_name`), which defeated
  `hook_affected_files`'s `*apply_patch*` gate — `guard-config`/`format`/
  `guard-secrets` write-deny were silent no-ops on Codex. Fixed + regression-
  tested, shipping as **v0.4.1** ahead of this repackage. Distribution facts
  (`codex plugin marketplace add <local path>`; `hooks` feature stable) and
  the `.codex-plugin/plugin.json` / `.agents/plugins/marketplace.json` schemas
  confirmed against the live CLI and generated schemas.
- 2026-07-10 — Hardened after two-agent review: verification grep made
  satisfiable, Codex distribution facts routed through a stamped matrix row
  (scope item 8), pointer to off-repo planning context removed, value claim
  reconciled with the README's existing manual install path.
- 2026-07-10 — Plan brought into the repo's plans structure; not started.

## Decisions

- 2026-07-10 — Capture over waive (carry-over debt retired for real). Method:
  `codex exec --dangerously-bypass-hook-trust` in a scratch repo with a
  tee hook, driving one `cat` read + one apply_patch edit. Findings: (a) real
  apply_patch command is the bare `*** Begin Patch` … `*** End Patch` body,
  no `apply_patch` literal — the fixtures' wrapper form was wrong, which is
  why CI stayed green on a guard bypass; (b) `tool_name` carries the tool
  identity (`apply_patch` for edits, `Bash` for shell); (c) Stop payload has
  `stop_hook_active` + `last_assistant_message`; Stop output accepts
  `{"continue":true}` / `{}` — advise-once protocol validated. Resolution:
  fix in v0.4.1 (gate on the envelope marker), bare-envelope regression tests
  added. No `apply_patch|shell` alternation needed — the PreToolUse guards are
  matcher-less (see both), and `format.sh`'s `apply_patch` matcher matches the
  real `tool_name`.
- 2026-07-10 — Scope locked; execution is mechanical. Codex plugin/
  marketplace schemas were checked against live docs during scoping, but no
  citation landed in-repo — hence scope item 8 makes the matrix carry the
  stamped facts before this plan's claims can be considered verified.
- 2026-07-10 — Two sequential PRs (0.4.0 correctness, then 0.5.0 repackage)
  per migrations.md's never-migrate-and-upgrade-together rule.

## Next action

Land **v0.4.1** (the apply_patch guard-bypass fix) first, then execute scope
items 1–10 in order. The capture entry criterion is satisfied.
