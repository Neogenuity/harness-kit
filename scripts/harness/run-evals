#!/usr/bin/env bash
# run-evals — run the behavioral golden-task evals (multi-trial pass@k over isolated workspaces)
# Thin command entry: the implementation is kit mechanism in lib/.
exec bash "$(dirname "$0")/lib/eval.sh" "$@"
