#!/usr/bin/env bash
# sync — regenerate provider skill/agent stubs + generated .harness/adapters/
# from the canonical docs (--check = drift detection). `sync secrets [--check]`
# regenerates the native secret-deny mirrors from SECRET_PATTERNS (jq required).
# Thin command entry: the implementation is kit mechanism in lib/.
exec bash "$(dirname "$0")/lib/sync-lib.sh" "$@"
