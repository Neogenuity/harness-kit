#!/usr/bin/env bash
# sync — regenerate provider skill/agent stubs from the canonical docs (--check = drift detection)
# Thin command entry: the implementation is kit mechanism in lib/.
exec bash "$(dirname "$0")/lib/sync-lib.sh" "$@"
